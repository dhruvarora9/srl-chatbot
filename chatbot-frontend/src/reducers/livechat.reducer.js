const initialState = {
  loading: false,
  messageLoading: false,
  messages: [],
  roomId: null,
};

const livechat = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default livechat;
